"""Extroc.

A conversational decision aid for an airline Crew Control desk.

The language model plans and explains. It never produces a fact and never does
arithmetic. See the root `CLAUDE.md` for the boundary this package enforces.
"""

__version__ = "0.1.0"
